-- ============================================================================
-- مقترح توسعة enum public.event_type — أنواع أحداث الفصل غير المُمثَّلة حالياً
-- الحالة: *** غير مُطبَّق على قاعدة البيانات الحيّة *** (يتطلب موافقة صريحة)
-- التاريخ: 2026-06-28 · النطاق: Sprint 1 — تشخيص فقط، بلا تطبيق
-- ============================================================================
--
-- السبب (لماذا لا يكفي app-code وحده):
--   enum public.event_type يقبل حالياً 7 قيم فقط (تحقّق حيّ):
--     غياب · استئذان · تحويل إلى وكيل شؤون الطلاب · مخالفة · تأخر · زيارة عيادة · زيارة مصادر تعلم
--   بينما واجهة الفصل تُنتج أنواعاً إيجابية/مكافآت وخروج دورة مياه لا تطابق أياً منها.
--   في Sprint 1 عالجنا هذا في طبقة التطبيق بـ mapToDbEventType:
--     • غائب→غياب · متأخر→تأخر · المخالفات الصفّية المتعددة→مخالفة (التفصيل في note/metadata)
--     • عيادة→زيارة عيادة · مصادر التعلم→زيارة مصادر تعلم
--   أمّا الأنواع التالية فلا يمكن تمثيلها بصدق بأي قيمة موجودة (تعيينها لقيمة تأديبية تزوير
--   دلالي)، لذا التطبيق *يرفضها بصدق* بدل كتابة قيمة خاطئة. تفعيلها يتطلب تغيير schema:
--
--   (أ) أحداث إيجابية/مكافآت ثابتة → تحتاج قيم enum جديدة:
--       شارك اليوم · تفكير إبداعي · مبادرة/قيادة · التزام وانضباط · تميز ملحوظ
--       نجم الحصة 1 · نجم الحصة 2 · نجم الحصة 3
--   (ب) خروج/عودة دورة المياه → قيم enum جديدة (أو الأفضل: استخدام جدول classroom_exits
--       المخصّص — لكنه يتطلب class_id NOT NULL، وهو غير متوفّر في تدفّق useClassroom الحالي):
--       دورة المياه - خرج · دورة المياه - عاد
--   (ج) الأوسمة `وسام: <type>` ديناميكية (لا تصلح قيم enum ثابتة) → تحتاج جدول مكافآت/أوسمة
--       مستقلاً (مثل student_badges) لا توسعة enum.
--
-- ملاحظات تطبيق (عند الموافقة لاحقاً):
--   • ALTER TYPE ... ADD VALUE لا يعمل داخل transaction block ولا يمكن التراجع عنه.
--   • تنفَّذ كل جملة على حدة، ثم تُحدَّث mapToDbEventType في lib/types/classroom.ts لتمرير
--     القيم الجديدة مباشرة، ويُفعَّل saveStarsAction/awardBadge.
--   • لا تُطبَّق هذه القيم ما لم تُعتمد المسمّيات الرسمية من مالك المنتج.
--
-- ----------------------------------------------------------------------------
-- (أ) أحداث إيجابية/مكافآت ثابتة:
-- ALTER TYPE public.event_type ADD VALUE IF NOT EXISTS 'شارك اليوم';
-- ALTER TYPE public.event_type ADD VALUE IF NOT EXISTS 'تفكير إبداعي';
-- ALTER TYPE public.event_type ADD VALUE IF NOT EXISTS 'مبادرة/قيادة';
-- ALTER TYPE public.event_type ADD VALUE IF NOT EXISTS 'التزام وانضباط';
-- ALTER TYPE public.event_type ADD VALUE IF NOT EXISTS 'تميز ملحوظ';
-- ALTER TYPE public.event_type ADD VALUE IF NOT EXISTS 'نجم الحصة 1';
-- ALTER TYPE public.event_type ADD VALUE IF NOT EXISTS 'نجم الحصة 2';
-- ALTER TYPE public.event_type ADD VALUE IF NOT EXISTS 'نجم الحصة 3';
--
-- (ب) خروج/عودة دورة المياه (بديل أنظف: ربط تدفّق الخروج بجدول classroom_exits بعد توفير class_id):
-- ALTER TYPE public.event_type ADD VALUE IF NOT EXISTS 'دورة المياه - خرج';
-- ALTER TYPE public.event_type ADD VALUE IF NOT EXISTS 'دورة المياه - عاد';
--
-- (ج) الأوسمة: لا توسعة enum — يلزم جدول مكافآت مستقل (تصميم منفصل، خارج هذا الملف).
-- ============================================================================
