# قوالب الجودة الخاصة بالمستأجر — Tenant Quality Templates

> **النوع:** دستور — يصحّح فهماً جوهرياً حول ملكية قوالب الجودة.
> **الأهمية:** **حرِج.** سوء فهم هذه النقطة يكسر تعدّد المستأجرين أو يفرض هوية مدرسة على كل المدارس.
> **العلاقة:** يخدم [SIDRA_SYSTEM_DOCTRINE](../architecture/SIDRA_SYSTEM_DOCTRINE.md) §2 (التسمية الديناميكية) و[QUALITY_FORMS_AND_AUTOFILL_LAYER](./QUALITY_FORMS_AND_AUTOFILL_LAYER.md) §2.
> **آخر تحديث:** 2026-06-12.

---

## 1) التصحيح الحاسم

> **نماذج/أكواد «مدارس الفلاح الأهلية» الموجودة حالياً في الكود (مثل أكواد `QF-*`، الترويسات، أسماء النماذج) هي قوالب خاصة بمستأجر «الفلاح» — وليست افتراضات سِدرة العالمية.**

- طبقة «نماذج الجودة» **قدرة عالمية** في منصّة سِدرة.
- لكن **القالب · الكود · الترويسة · شعار المدرسة · لغة النموذج · الإتاحة (متى يظهر النموذج لمن)** كلها **لكل مدرسة / لكل مستأجر** (`school_id`-scoped).
- **مدرسة بلا برنامج جودة لا ترى نماذج الفلاح.** عرضها لها = تسريب هوية مستأجر + افتراض خاطئ بأن نظام جودة الفلاح هو نظام سِدرة العالمي.

---

## 2) ما هو عالمي وما هو خاص بالمستأجر

| العنصر | عالمي (سِدرة) | خاص بالمستأجر (لكل مدرسة) |
| --- | --- | --- |
| **القدرة** (وجود طبقة نماذج جودة) | ✅ | |
| **محرّك التعبئة + التصدير PDF** (الآلية) | ✅ | |
| **اسم المنتج المعروض** («سِدرة») | ✅ | |
| **قائمة القوالب المتاحة** | | ✅ |
| **أكواد النماذج** (مثل `QF-70-j-4-1`) | | ✅ |
| **ترويسة/شعار/اسم المدرسة في PDF** | | ✅ (من `schools.name`) |
| **تفعيل/تعطيل وحدات الجودة** | | ✅ |
| **لغة/صياغة بنود النموذج** | | ✅ |

---

## 3) الاستثناء المشروع (والمهم)

**مقبول تماماً** أن يَعرض قالب **خاص بمستأجر الفلاح** الاسم «مدارس الفلاح الأهلية» — **بشرط واحد:**

> أن يكون مُنمذَجاً صراحةً كـ **tenant-specific template** (مرتبط بـ `school_id` الخاص بالفلاح) — **لا** كنصّ مُثبَّت في قالب عام قابل لإعادة الاستخدام عبر كل المدارس.

الفرق الجوهري:

```
❌ خطأ (كسر مستأجر):  ثابت في مكوّن عام →  <Text>مدارس الفلاح الأهلية</Text>
✅ صحيح (ديناميكي):   <Text>{school.name}</Text>   // يقرأ اسم مستأجر المستخدم الحالي
✅ صحيح (قالب مستأجر): قالب مرتبط بـ school_id = «الفلاح» قد يحمل اسم الفلاح بحكم خصوصيته
```

---

## 4) الحالة الحالية (صدق) والفجوة

- ✅ **مُصلَح (Phase 3B):** `app/health/_components/HealthReports.tsx` لم يَعُد يُثبّت «مدارس الفلاح الأهلية» — `schoolName` يُمرَّر الآن ديناميكياً من سياق المستأجر المصادَق. أُزيلت الأسماء المُثبَّتة من قوالب PDF المشتركة (Activity/Secretary أيضاً).
- ✅ **مُطبَّق (Phase 3D):** القوالب مُنمذَجة tenant-specific في `lib/quality/tenant-templates.ts`، وقوالب الفلاح مُخفاة عن أي مدرسة غير مُسجَّلة (fail-closed عبر `QualityDisabledNotice`).
- **متبقٍّ:** اعتماد القوالب الرسمية للأدوار الأربعة المخطّطة + بناء قوالب LRC الثمانية + خط عربي لأي مولّد jsPDF متبقٍّ (المرحلة 3، انظر §8).

---

## 5) قاعدة للمساهمين

1. **لا تُثبّت** اسم أي مدرسة في قالب/مكوّن عام. اقرأه من سياق المستأجر.
2. **لا تفترض** أن أكواد/نماذج الفلاح متاحة لكل المدارس.
3. عند بناء طبقة قوالب الجودة (المرحلة 3): اجعل **القالب كياناً مرتبطاً بـ `school_id`**، والإتاحة قراراً لكل مدرسة.
4. اقرأ [SIDRA_SYSTEM_DOCTRINE](../architecture/SIDRA_SYSTEM_DOCTRINE.md) §2 قبل لمس أي قالب أو ترويسة PDF.

---

## 6) السجلّ البرمجي (Phase 3D) — `lib/quality/tenant-templates.ts`

نُفِّذ سجلّ app-code يُنمذج هذه القواعد (بلا قاعدة بيانات):

- **`AL_FALAH_QUALITY_TEMPLATES`** — أكواد QF الخاصة بمستأجر الفلاح (health/secretary/student_affairs/counseling/activity) موسومة `tenantSpecific: true`. **مصدر الحقيقة للأكواد**.
- **`GLOBAL_QUALITY_TEMPLATES`** — كتالوج عالمي بلا أكواد QF خاصة (LRC من `quality-forms.ts` + شهادة الانضباط العامة) موسوم `tenantSpecific: false`.
- **`TENANT_QUALITY_REGISTRY`** (`school_id → TenantQualityConfig`) — **فارغ افتراضياً (fail-closed)**؛ PRE-LAUNCH لا مدارس حقيقية. تسجيل برنامج مدرسة يضيف إدخالاً — **لا تُسجَّل قوالب الفلاح لمعرّف مجهول**.
- **الوصول الآمن:** `getTenantQualityConfig` · `isQualityEnabled` · `getQualityTemplates(schoolId, module?)` · `isQualityModuleEnabled` · `getTemplateByCode` — كلها fail-closed (`schoolId` غائب/غير مُسجَّل → معطّل/قائمة فارغة).
- **الإتاحة لكل مدرسة app-code فقط** (لا feature flags في DB بعد — طبقة DB لكل مدرسة = Phase 3F).

### حالة الربط (wiring) — مُحدَّثة 3D-2/3D-3
- **3D-2:** سُجِّلت مدرسة الفلاح (`bfe99c43-…` من سجلّ Supabase الحقيقي) في `TENANT_QUALITY_REGISTRY`، ورُبطت مُستهلِكات: health/activity ExportButtons · secretary ReportsCenter · counselor QualityForms عبر `isQualityModuleEnabled`.
- **3D-3:** رُبطت بقية أسطح QF المالكة: LRC (`LrcQualityForms`) · QA corrective-action (`/qa/corrective-action` + `/new`، server-side عبر `getActivePersona().schoolId`، وحدة `qa` جديدة + قالب QF03-1) · student-affairs quality forms (تحصين مستقبلي — غير مرسومة بالصفحة بعد).
- **لوحات مخطّطة مُضافة (3D-4):** lab_technician (`/science`) · school_principal (`/principal`) · school_admin (`/school/[id]/dashboard`) · academic_vp (`/educational`) — لها الآن لوحات حالة مُبوّبة بالسجلّ (`QualityOwnerPanel`) تعرض قوالبها كـ«قيد الاعتماد» (planned · `implemented:false` · بلا رمز QF) حتى تُعتمد القوالب الرسمية من مالك المنتج. **بلا أزرار تصدير ولا PDF للمخطّط.**
- **الأكواد الحرفية داخل مستندات React-PDF تبقى** (مُمرآة في السجلّ)؛ تُعرَض فقط عبر مُستهلِكات مُبوّبة بالسجلّ (للفلاح المُسجَّلة)، وحالة فارغة صادقة (`QualityDisabledNotice`) لأي مدرسة غير مُسجَّلة. `school_affairs_vp` مُستثنى (لا يُربَط).

---

## 7) التحرير لكل مستأجر بلا تغيير كود (Phase 3E-2) — `school_admin`

السجلّ app-code (`tenant-templates.ts`) يبقى **الافتراض** والمصدر الأساسي للأكواد؛ لكن **`school_admin` يخصّص قوالب مدرسته بلا تغيير كود** عبر طبقة DB:

### أ) توحيد الأكواد (canonical · alias · displayCode)
- `QualityTemplate` اكتسب `aliases?` (أكواد بديلة تاريخية تشير لنفس النموذج) و`displayCode?` (الرمز المعروض، افتراضه `code`).
- مثال محسوم: `QF19-1` (المعتمد) ⟷ `QF-19-1` (alias) = «دعوة لحضور اجتماع» نفسها؛ و`QF19-2` ⟷ `QF-19-2` = «محضر اجتماع رسمي». التوحيد **بالمطابقة لا بالاستبدال** — `getTemplateByCode` يطابق code/displayCode/alias؛ والتخزين دائماً بالرمز المعتمد.

### ب) إعدادات DB لكل مدرسة (M80) — يديرها `school_admin` لمدرسته فقط
| الجدول | الدور | المستوى |
| --- | --- | --- |
| `school_quality_settings` | افتراضات المدرسة (شعار · ترويسة · تذييل · هوية) | **كامل المدرسة** |
| `school_quality_template_overrides` (`scope='module'`) | تجاوز وحدة/دور (مثل نماذج المرشد فقط) | **وحدة/دور** |
| `school_quality_template_overrides` (`scope='form'`) | تجاوز نموذج بعينه (الرمز المعروض · العنوان · الشعار · التفعيل) | **نموذج واحد** |

- **الأسبقية (طبقة الخدمة `template-settings.ts`):** نموذج → وحدة → مدرسة → السجلّ app-code (fallback).
- **عزل المستأجر:** `school_id` من سياق مصادَق فقط · الكتابة لـ `school_admin` (مدرسته) أو `system_owner` (مُدعَّم بـ RLS جديد على الجدولين) · fail-closed للمجهول.
- **السجلّ التاريخي ثابت:** الإعدادات **لا تمسّ `generated_forms`** — السجلات المُولَّدة تحتفظ برمزها وحمولتها؛ الإعدادات للتوليد **المستقبلي** فقط.
- **`school_affairs_vp` يبقى مُستثنى** من ملكية نماذج الجودة (لا يُمنح إدارة الإعدادات بحكم الدور — `school_admin` فقط).

### ج) قاعدة للمساهمين (مُحدَّثة)
- تعريف القوالب/الأكواد **الافتراضية** يبقى في `tenant-templates.ts`؛ **تخصيص مدرسة** يكون عبر جداول M80 لا بتعديل كود.
- لا تُضِف «نمط رمز» عام يُحوّل الأكواد آلياً (دلالة ملتبسة عبر أكواد غير متجانسة)؛ تغيير الرمز المعروض يتم بدقّة عبر `display_code` على مستوى النموذج.

---

## 8) ملخّص حالة التنفيذ (مُحدَّث 2026-06-18)

- **المستأجر المُسجَّل:** مدرسة الفلاح فقط — `school_id = bfe99c43-fa5c-46f4-8ad0-05e12184b55e` (من سجلّ Supabase الحقيقي). أي مدرسة أخرى → **لا قوالب** (fail-closed).
- **أسطح جودة مُنفَّذة (`implemented: true`)، مُبوّبة بالسجلّ:** الصحة (health) · النشاط (activity) · السكرتارية (secretary) · الإرشاد (counseling) · مصادر التعلّم (LRC) · ضمان الجودة (QA — `QF03-1`) · شؤون الطلاب (student-affairs).
- **أسطح جودة مخطّطة (`implemented: false` · بلا رمز QF مُختلَق · «قيد الاعتماد» · بلا تصدير/PDF):** `school_principal` · `school_admin` · `academic_vp` · `lab_technician`. تُحدَّث إلى `implemented: true` فقط عند اعتماد القوالب الرسمية من مالك المنتج (مع رمز QF حقيقي).
- **مُستثنى من ملكية نماذج الجودة:** `school_affairs_vp` · `teacher` · `student` · `parent` (انظر `lib/quality/quality-forms.ts` · `QUALITY_FORM_OWNER_ROLES`).
- **أساس التعبئة/الأدلة (Phase 3E):** خدمات خادمية `lib/quality/{generated-forms,quality-evidence,autofill}.ts` (`school_id` من سياق مصادَق · قالب مُنفَّذ فقط · منع تكرار · fail-closed) + سلسلة الحضور→الدليل حيّة (M78 trigger → `quality_evidence` ATT-001).
- **صدق البيانات:** **ممنوع** أكواد QF مُختلَقة · PDF وهمي · أدلة وهمية · تحليلات/ذكاء وهمي. كل سطح غير جاهز يُعرَض «قيد الاعتماد» أو حالة فارغة صادقة.
