# طبقة نماذج الجودة ومحرّك التعبئة التلقائية — Quality Forms & Auto-Fill Layer

> **النوع:** دستور الطبقتين العرضيّتين (6) و(7).
> **العلاقة:** يفصّل العمود الفقري للتدفّق الرسالي في [SIDRA_SYSTEM_DOCTRINE §4](../architecture/SIDRA_SYSTEM_DOCTRINE.md). اقرأ الدستور أولاً.
> **حالة المشروع:** PRE-LAUNCH. **آخر تحديث:** 2026-06-12.

---

## 1) لماذا هاتان الطبقتان «عرضيّتان إلزاميّتان»؟

طبقة الجودة + محرّك التعبئة **يخترقان أغلب الأدوار** ويربطان السلسلة:

```
إجراء تشغيلي → بيانات منظَّمة → سجلّ سير عمل/تعبئة تلقائية → بيانات نموذج جودة → PDF رسمي → مؤشرات → ذكاء
```

بدونهما يتحوّل سِدرة إلى مجرد نماذج إدخال منفصلة. **بهما** يصبح **نظام تشغيل** ينتج وثائق رسمية قابلة للتدقيق وذكاء قرار من العمل الحقيقي.

---

## 2) القدرة عالمية، التطبيق لكل مستأجر

- **«نماذج الجودة» قدرة عالمية في المنصّة** — كل مدرسة قد تملك برنامج جودة.
- لكن **القوالب والأكواد والترويسات والإتاحة تُحدَّد لكل مدرسة/مستأجر**. مدرسة بلا برنامج جودة **لا ترى** نماذج مدرسة أخرى.
- التفصيل الحاسم في [TENANT_QUALITY_TEMPLATES](./TENANT_QUALITY_TEMPLATES.md).

---

## 3) محرّك التعبئة التلقائية (Auto-Fill Engine) — كيف يعمل

الإجراء التشغيلي **لا يُكتب يدوياً مرتين**. الفكرة: التقاط الحدث مرة، ثم **اشتقاق** سجلّ سير العمل وبيانات نموذج الجودة منه تلقائياً.

**المثال المرجعي — زيارة فصل لمركز مصادر التعلم (LRC):**

1. **معلّم يطلب** زيارة فصل للمركز (حجز).
2. **أمين المصادر يوافق** على الحجز.
3. عند الموافقة، **يُنشئ النظام زيارة فصل** (class visit) — لا إدخال يدوي منفصل.
4. **يُعبّئ النظام قائمة الطلاب تلقائياً** من كشف الفصل (class roster).
5. **يستبعد** الطلاب الغائبين/المأذونين/المعذورين **إن وُجد سجلّ حضور** لتلك الحصة (الأعمدة موجودة: `is_present` / `is_excluded`).
6. بيانات الزيارة **تُغذّي نماذج جودة LRC** وتُخرَج في **PDF رسمي**.
7. الزيارة تُولّد **دليل جودة** (`quality_evidence`) يُغذّي المؤشرات والذكاء.

> **الحالة الحالية (صدق):** الخطوات 1–2 موجودة؛ الخطوات 3 (ربط الموافقة→الزيارة آلياً) و5 (الاستبعاد من حضور حقيقي) و7 (توليد دليل جودة من الزيارة) **مخطّطة وقابلة للبناء app-code** (الأعمدة موجودة — **بلا migration جديد**). راجع المرحلة 3/4 في [SIDRA_IMPLEMENTATION_ROADMAP](../roadmap/SIDRA_IMPLEMENTATION_ROADMAP.md).

---

## 4) القدرات الحقيقية الحالية لتوليد الوثائق (PDF)

| الوحدة | النماذج | المولّد | حالة العربية |
| --- | --- | --- | --- |
| Health (الصحة) | 4 نماذج QF | `@react-pdf/renderer` + Tajawal | ✅ آمنة |
| Activity (النشاط) | 7 نماذج (QF71-G-*) | `@react-pdf` | ✅ آمنة |
| Secretary (السكرتير) | 7+ نماذج | `@react-pdf` | ✅ آمنة |
| Student-Affairs (شؤون الطلاب) | 3+ (إحالات/حضور/عقود) | `@react-pdf` | ✅ آمنة |
| LRC (شهادة تميّز) | شهادة + 8 مخطّطة | `jsPDF` | ⚠️ خطر خط عربي |
| Discipline Knights | تقرير سلوكي | `jsPDF` | ⚠️ خطر خط عربي |
| Edge Function (QMS) | عام | `pdf-lib` | ⚠️ خطر خط عربي |

**حقيقي ومُنفَّذ:** كل ما سبق يُولّد PDF فعلياً (لا وهمي).

---

## 5) متطلبات الوثيقة الرسمية (PDF Requirements) — معيار إلزامي

كل PDF رسمي يجب أن يستوفي:

1. **عربية RTL سليمة** — لا حروف مكسورة/مقلوبة. (`@react-pdf`+Tajawal آمن؛ jsPDF/pdf-lib **يلزمهما تضمين خط عربي** مثل Noto — وإلا Helvetica تكسر العربية.)
2. **ترويسة رسمية باسم مدرسة ديناميكي** — من `schools.name` للمستأجر الحالي، **لا اسم مُثبَّت**.
3. **كود النموذج** (مثل `QF-70-j-4-1`) — حسب قالب المستأجر.
4. **تاريخ التوليد** الفعلي.
5. **خانات التوقيع/الاعتماد** حسب النموذج.
6. **بيانات حقيقية فقط** — لا أرقام مُختلَقة (قاعدة صدق البيانات §3 في الدستور).

---

## 6) الفجوات المعروفة (تُعالَج في المراحل 3/4 — لا الآن)

- **اسم مدرسة مُثبَّت** في بعض قوالب PDF (مثل `HealthReports.tsx:103` «مدارس الفلاح الأهلية») → يجب تمريره ديناميكياً (`schoolName` prop = app-code، بلا schema).
- **خط عربي مفقود** في jsPDF/pdf-lib → تضمين Noto.
- **8 قوالب LRC «قريباً»** (البيانات جاهزة، القوالب غير مبنيّة).
- **ربط الموافقة→الزيارة + استبعاد الغياب** غير مُفعّل بعد (app-code).
- **3 سياقات ذكاء فارغة** (`class_report` · `behavior_pattern` · `quality_summary`).

> **لا يُعالَج أيٌّ من هذا في المرحلة 1 (التوثيق).** التوثيق يثبّت المعيار؛ التنفيذ في المراحل 3–5.

---

## 7) طبقة خدمة التعبئة والأدلة (Phase 3E) — app-code

نُفِّذت طبقة خدمة **خادمية** تُجسّد السلسلة (إجراء تشغيلي → سجل نموذج → دليل):

- **`lib/quality/generated-forms.ts`** — `createGeneratedForm()`: يُنشئ سجل `generated_forms` **لقالب مُنفَّذ فقط**. `school_id` من `getActivePersona` (**لا من العميل**) · بوّابة سجلّ المستأجر (planned/مجهول → fail-closed) · منع تكرار (`school_id` · `form_code` · `source_record_id`). لا يُولّد PDF (التوليد عبر Edge عند `is_ready`).
- **`lib/quality/quality-evidence.ts`** — `createQualityEvidence()`: يُنشئ `quality_evidence` **لمؤشر مزروع حقيقي فقط**. fail-closed: مؤشر غير مزروع → `indicator_not_seeded` · لا سنة نشطة → `no_active_academic_year` · جودة غير مفعّلة → `quality_disabled`. `source_module` مقيّد بـ CHECK. RLS: الإدراج عبر العميل لـ `quality_coordinator`/`school_admin` فقط؛ الأدلة الآلية من وحدات أخرى عبر triggers `SECURITY DEFINER` (M78).
- **`lib/quality/autofill.ts`** — حارس `isImplementedTemplate()` + نوع `AutoFillPayload` (**نقي**، بلا DB — يصحّ للعميل والخادم).

### المُفعَّل فعلياً (flow مربوط)
- **qa — الإجراء التصحيحي (`QF03-1`):** `wizard-service` يُنشئ سجل `generated_forms` الآن عبر `createGeneratedForm` (مُبوّب بسجلّ المستأجر + منع تكرار). best-effort — لا يُفشِل الإجراء التصحيحي.

### غير مُفعَّل بعد (بصدق — ولماذا)
- **توليد الأدلة (`quality_evidence`) خامل:** `quality_indicators`=0 و`academic_years`=0 في DB الحية → أي إدراج دليل يفشل بالضرورة. **الزرع خطوة DB منفصلة تتطلب موافقة** (لم تُنفَّذ في 3E). الخدمة جاهزة fail-closed لحين الزرع — **بلا أدلة وهمية**.
- **الاجتماعات (`meeting-service`: `QF-19-1`/`QF-19-2`):** أكوادها لا تطابق أكواد السجلّ (`QF19-1`/`QF19-2`) → تبويبها عبر الخدمة سيحجبها؛ يلزم توحيد الأكواد أولاً (مؤجَّل).
- **تصديرات React-PDF التجميعية (health/secretary/activity/student-affairs):** تجميعية بلا `source_record_id` مفرد (`NOT NULL`)، وعميلية؛ ربطها بـ `generated_forms` يتطلب server action + نموذج سجل-واحد (مؤجَّل).

> **القاعدة:** لا سجل/دليل رسمي إلا لقالب **مُنفَّذ** ومدرسة **مُسجَّلة**؛ planned/مجهول → fail-closed. **لا دليل بلا بيانات مصدر حقيقية.**
